import React from 'react';
import { Text } from 'react-native';

import { ScreenContainer } from './ScreenContainer';

export const Splash = () => (
    <ScreenContainer>
        <Text>Loading...</Text>
    </ScreenContainer>
);
