import React from 'react';
import { Text } from 'react-native-paper';

import { ScreenContainer } from 'ui/common';

export const Main = () => {
    return (
        <ScreenContainer>
            <Text>Main Screen of the App</Text>
        </ScreenContainer>
    );
};
